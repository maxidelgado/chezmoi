[data]
    # -- Prompts on `chezmoi init` --
    system_username = {{ promptString "system_username" "Your System Username" }}
    company = {{ promptString "company" "Your Company" }}
    first_name = {{ promptString "first_name" "Your First Name" }}
    last_name = {{ promptString "last_name" "Your Last Name" }}
    personal_email = {{ promptString "personal_email" "Your Personal Email" }}
    work_email = {{ promptString "work_email" "Your Work Email" }}
    aws_region = {{ promptString "aws_region" "Enter AWS region (e.g., us-east-1, us-west-2)" }}

[edit]
    command = "nvim"
